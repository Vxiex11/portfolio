dy .cmd .path{color:var(--blue)}
  .term-body .k{color:var(--violet-hi)}
  .term-body .r{color:var(--red)}
  .term-body .b{color:var(--blue)}
  .term-body .g{color:var(--ok)}
  .term-body .dim{color:var(--muted-2)}
  .term-input-line{display:flex;align-items:center;font-family:var(--mono);font-size:.86rem;gap:8px}
  .term-input-line .ps{color:var(--ok)}
  .term-input-line .path{color:var(--blue)}
  #term-input{background:transparent;border:none;outline:none;color:var(--text);font-family:var(--mono);font-size:.86rem;flex:1;caret-color:var(--violet)}
  .chips{display:flex;flex-wrap:wrap;gap:8px;margin-top:16px}
  .chip{font-family:var(--mono);font-size:.74rem;color:var(--muted);padding:6px 12px;border:1px solid var(--line);border-radius:20px;background:var(--surface);cursor:pointer;transition:.15s}
  .chip:hover{color:var(--violet-hi);border-color:var(--violet)}
  .term-hint{font-family:var(--mono);font-size:.72rem;color:var(--muted-2);margin-top:12px}

  /* ABOUT */
  .about-grid{display:grid;grid-template-columns:1.4fr 1fr;gap:48px}
  @media(max-width:800px){.about-grid{grid-template-columns:1fr;gap:32px}}
  .about p{color:#c4c4d4;margin-bottom:18px;max-width:58ch}
  .about p .em{color:var(--violet-hi);font-weight:500}
  .about-side{border-left:1px solid var(--line);padding-left:26px}
  .about-side .row{margin-bottom:22px}
  .about-side .lab{font-family:var(--mono);font-size:.72rem;color:var(--muted-2);margin-bottom:5px}
  .about-side .val{font-size:.95rem;color:var(--text)}
  .about-side .val a{border-bottom:1px solid var(--line-strong)}
  .about-side .val a:hover{color:var(--violet-hi)}

  /* EXPERIENCE */
  .xp{position:relative;padding-left:34px}
  .xp::before{content:"";position:absolute;left:8px;top:6px;bottom:6px;width:2px;background:linear-gradient(180deg,var(--red),var(--violet) 55%,var(--blue))}
  .xp-item{position:relative;margin-bottom:44px}
  .xp-item:last-child{margin-bottom:0}
  .xp-item .node{position:absolute;left:-33px;top:5px;width:18px;height:18px;border-radius:50%;background:var(--bg);border:2px solid var(--violet);display:grid;place-items:center}
  .xp-item .node::after{content:"";width:6px;height:6px;border-radius:50%;background:var(--violet)}
  .xp-item.cur .node{border-color:var(--red)}
  .xp-item.cur .node::after{background:var(--red);box-shadow:0 0 12px var(--red)}
  .xp-top{display:flex;align-items:baseline;gap:12px;flex-wrap:wrap;margin-bottom:6px}
  .xp-top h3{font-size:1.2rem;font-weight:600}
  .xp-top .dur{font-family:var(--mono);font-size:.75rem;color:var(--muted)}
  .xp-top .badge{font-family:var(--mono);font-size:.68rem;padding:3px 9px;border-radius:20px;border:1px solid rgba(255,69,96,.4);color:var(--red);background:rgba(255,69,96,.08)}
  .xp-org{font-family:var(--mono);font-size:.82rem;color:var(--violet-hi);margin-bottom:12px}
  .xp-item ul{list-style:none;display:flex;flex-direction:column;gap:8px}
  .xp-item li{color:#bcbccc;font-size:.95rem;padding-left:20px;position:relative;max-width:66ch}
  .xp-item li::before{content:"\2023";position:absolute;left:0;color:var(--muted-2)}

  /* PROJECTS */
  .projects{display:flex;flex-direction:column;gap:20px}
  .proj{display:grid;grid-template-columns:1fr auto;gap:24px;align-items:start;border:1px solid var(--line);border-left:3px solid var(--accent,var(--violet));border-radius:8px;padding:28px;background:var(--surface);transition:border-color .18s,background .18s}
  .proj:hover{background:var(--surface-2);border-color:var(--line-strong);border-left-color:var(--accent,var(--violet))}
  @media(max-width:720px){.proj{grid-template-columns:1fr;gap:16px}}
  .proj .kind{font-family:var(--mono);font-size:.72rem;color:var(--accent,var(--violet));margin-bottom:10px;display:inline-block}
  .proj h3{font-size:1.3rem;font-weight:600;margin-bottom:10px}
  .proj p{color:#bcbccc;font-size:.95rem;max-width:62ch;margin-bottom:14px}
  .proj .stack{display:flex;flex-wrap:wrap;gap:7px}
  .proj .stack span{font-family:var(--mono);font-size:.7rem;color:var(--muted);border:1px solid var(--line);padding:4px 9px;border-radius:4px}
  .proj .go{font-family:var(--mono);font-size:.8rem;color:var(--accent,var(--violet));white-space:nowrap;align-self:center;border:1px solid var(--line-strong);padding:10px 16px;border-radius:5px;transition:.15s}
  .proj .go:hover{background:var(--accent,var(--violet));color:#0a0a12}
  @media(max-width:720px){.proj .go{justify-self:start}}
  .more-repos{margin-top:26px;font-family:var(--mono);font-size:.85rem;color:var(--muted)}
  .more-repos a{color:var(--violet-hi);border-bottom:1px solid var(--line-strong)}

  /* SKILLS */
  .skills-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:18px}
  @media(max-width:760px){.skills-grid{grid-template-columns:1fr}}
  .skill-block{border:1px solid var(--line);border-radius:8px;padding:24px;background:var(--surface);border-top:2px solid var(--tone,var(--violet))}
  .skill-block h3{font-size:1.02rem;font-weight:600;margin-bottom:16px;display:flex;align-items:center;gap:10px}
  .skill-block h3 .tick{width:8px;height:8px;border-radius:2px;background:var(--tone,var(--violet))}
  .skill-block .tags{display:flex;flex-wrap:wrap;gap:8px}
  .skill-block .tags span{font-family:var(--mono);font-size:.76rem;color:#c0c0d0;background:rgba(255,255,255,.03);border:1px solid var(--line);padding:6px 11px;border-radius:5px}

  /* TRACK RECORD */
  .ach-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:16px;margin-bottom:34px}
  @media(max-width:800px){.ach-grid{grid-template-columns:repeat(2,1fr)}}
  .ach{border:1px solid var(--line);border-radius:8px;padding:22px;background:var(--surface)}
  .ach .n{font-family:var(--mono);font-size:1.7rem;font-weight:700;color:var(--violet-hi);line-height:1}
  .ach .l{font-size:.82rem;color:var(--muted);margin-top:10px}
  .cert-row{display:flex;flex-wrap:wrap;gap:14px}
  .cert{display:flex;align-items:center;gap:12px;border:1px solid var(--line);border-radius:8px;padding:14px 18px;background:var(--surface);font-size:.9rem}
  .cert .st